# API service configuration
# Shared credentials (Supabase, OpenAI, optional integrations) are documented in docs/env-matrix.md
# and should be defined once in the root .env file or injected at runtime. Keep the ordering in
# this file aligned with the root .env.example for easier diffing.

# Core runtime
NODE_ENV=development
PORT=3333
LOG_LEVEL=info

# Agent runtime defaults
AGENT_MODEL=gpt-5-pro
EMBEDDING_MODEL=text-embedding-3-large
EMBEDDING_DIMENSION=
SUMMARISER_MODEL=
MAX_SUMMARY_CHARS=8000
AGENT_STUB_MODE=auto

# Governance / policy tagging
POLICY_VERSION=
