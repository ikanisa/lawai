# Ops tooling configuration
# Shared Supabase/OpenAI credentials and optional integrations are covered in docs/env-matrix.md.
# Define them in the root .env or your deployment secrets store. Mirror shared ordering with the
# root .env.example to keep updates consistent.

# Core runtime
NODE_ENV=development

# Safety toggles
OPS_CHECK_DRY_RUN=0
VECTOR_STORE_DRY_RUN=0

# API location
API_BASE_URL=http://localhost:3333/api

# Default identifiers (override per deployment)
OPS_ORG_ID=00000000-0000-0000-0000-000000000000
EVAL_ORG_ID=00000000-0000-0000-0000-000000000000
EVAL_USER_ID=00000000-0000-0000-0000-000000000000
API_BASE_URL=http://localhost:3333/api
