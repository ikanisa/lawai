import { describe, it } from 'vitest';

describe.skip('admin panel smoke flows', () => {
  it('signs in as admin, toggles policy, triggers ingestion, approves HITL, rollbacks workflow', () => {
    // Placeholder for full e2e coverage when Playwright harness is connected.
  });
});
